<h1>Song Form</h1>

<%= form_for @song do |f|%>

<%= f.label 'Song Name:' %><br>
<%= f.text_field :name %>

<%= f.label :genre_id %><br>
<%= f.text_area :genre_id %>

<%= f.label :artist_id %><br>
<%= f.text_area :artist_id %>

<%= f.submit %>
<%end%>
